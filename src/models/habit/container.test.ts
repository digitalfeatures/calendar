import { Habit } from './habit'

import { HabitContainer } from './container'
import { TodoItem } from '../todo'

describe('the unit test of contaniner', () => {
    it('the weekly periodic', () => {
        const container = new HabitContainer()

        // const habit = new Habit('My\'s Habit', 'the uint test function')

        // habit.setRepeat(HabitPeriodicType.DAILY)

        // container.insert(habit)

        // const content = container.toEncode()

        // const _container = new HabitContainer()

        // _container.toDecode(content)

        // expect(container.habits[0].uuid).toEqual(_container.habits[0].uuid)
    })
})
import { TodoItem } from '../todo';
import { Habit } from './habit';

describe('the unit test of periodic', () => {
    it('the weekly periodic', () => {
        // const habit = new Habit('My\'s Habit', 'the uint test function');
        // habit.setRepeat(HabitPeriodicType.WEEKLY)

        // const items: TodoItem[] = []

        // const todo = habit.handleInspect(items)

        // if (todo) {
        //     items.push(todo)
        // }

        // const _todo = habit.handleInspect(items)

        // expect(_todo?.uuid).toBe(todo?.uuid)
    })

    it('the daily periodic', () => {
        // const habit = new Habit('My\'s Habit', 'the uint test function');

        // const items: TodoItem[] = []

        // habit.setRepeat(HabitPeriodicType.DAILY)

        // const todo = habit.handleInspect(items)

        // if (todo) {
        //     items.push(todo)
        // }

        // const _todo = habit.handleInspect(items)

        // expect(_todo?.uuid).toBe(todo?.uuid)
    })

})

